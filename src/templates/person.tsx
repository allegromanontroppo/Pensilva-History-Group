/* eslint-disable no-unused-vars */
import React from 'react';
import Layout from '../components/layout';

const PersonTemplate: React.FC = () => {
	return (
		<Layout>
			<div>Hello PersonTemplate</div>
		</Layout>
	);
};

export default PersonTemplate;
