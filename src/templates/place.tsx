/* eslint-disable no-unused-vars */
import React from 'react';
import Layout from '../components/layout';

const PlaceTemplate: React.FC = () => {
	return (
		<Layout>
			<div>Hello PlaceTemplate</div>
		</Layout>
	);
};

export default PlaceTemplate;
